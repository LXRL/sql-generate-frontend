export type loginType = {
    userAccount: string,
    userPassword: string
}

export type signType = {
    userName: string,
    userAccount: string,
    userPassword: string,
    checkPassword: string
}